export const packmanColors = Object.freeze({
  white: '#fff',
  mint: '#30ccd8',
  gray: '#D9D9D9',
  someGray: '#ededed',
  lightGray: '#808080',
});

export type PackmanColorName = keyof typeof packmanColors;
